import styled from 'styled-components'

const Container = styled.main`
    width: 100%;

    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 19px;
`

export { Container }
